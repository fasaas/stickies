import AppCommands from '../../../src/commands/AppCommands'

describe('App Commands', () => {
    describe('Save', () => {
        describe('Given AsyncStorage save fails', () => {
            test('Return failed', async () => {})
        })
    })
})
